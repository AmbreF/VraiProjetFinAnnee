using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[System.Serializable]

//enrégistre les données utile pour la save

public class Player_Data
{
    /*
    public int money;
    public int product1;
    public int product2;
    public int product3;
    public int product4;
    public int product5;
    public int customers;
    public bool isTime2Upgraded;
    public bool isSellingTimeUpgraded;
    public bool isBoostUpgraded;
    public bool isWavesUpgraded;
    public bool isMaxUpgraded;
   
    public Player_Data(Player playerData)
    {
        money = playerData._controller.GetIdleModel().GetDrachmes().GetValue();
        product1 = playerData._controller.GetIdleModel().GetProduct1();
        product2 = playerData._controller.GetIdleModel().GetProduct2();
        product3 = playerData._controller.GetIdleModel().GetProduct3();
        product4 = playerData._controller.GetIdleModel().GetProduct4();
        product5 = playerData._controller.GetIdleModel().GetProduct5();
        customers = playerData._controller.nbCustomer;
        isBoostUpgraded = playerData._controller.isBoostUpgraded;
        isSellingTimeUpgraded = playerData._controller.isSellingTimeUpgraded;
        isTime2Upgraded = playerData._controller.isTime2Upgraded;
        isWavesUpgraded = playerData.isWavesUpgraded;
        isMaxUpgraded = playerData.isMaxUpgraded;
    }
    */
}
